/*import { Link } from 'react-router-dom';
const Header = ({ isLoggedIn, username, profilePicture, onSignin }) => {
  return (
    <header className="App-header">
      <Link to="/">Welcome, {props.name || 'Guest'}</Link>

      {isLoggedIn ? (
        <div className="profile">
          <img src={profilePicture} alt="Profile" />
          <p>{username}</p>
        </div>
      ) : (
        <button onClick={onSignin} disabled={isLoggedIn}>
          Sign In
        </button>
      )}
    </header>
  );
};

export default Header;
*/